
# Sardine API Configuration
# Place this file in the project root directory (not in config/)

# Your Sardine Client ID (safe to expose)
SARDINE_CLIENT_ID=your-client-id-here

# Your Sardine Client Secret (keep this secret!)
SARDINE_CLIENT_SECRET=your-client-secret-here

# API Endpoint URL
SARDINE_API_URL=https://api.sandbox.sardine.ai/v1/businesses

# Rate limit delay in seconds between requests (default: 0.5)
# Each worker respects this delay
RATE_LIMIT_DELAY=1.2

# Number of parallel worker processes (default: 4)
# Higher = faster, but may hit API rate limits
# Recommended: Start with 4, increase to 6-8 if no issues
NUM_PROCESSES=4